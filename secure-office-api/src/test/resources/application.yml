server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/secure_office
    username: secure_user
    password: secure_pass
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
